<h1>req</h1>
<%= form_for(@req) do |f| %>
  <% if @req.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@req.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% @req.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patio %><br />
    <%= f.check_box :patio %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h3><%= link_to 'Result', result_path %></h3>